#!/bin/zsh
if [[ $# = 0 ]]; then
    print "Specify file to run"
    exit -1
fi

case ${1:e} in
py)
    [[ -d ".venv" ]] && . .venv/bin/activate
    python3 $1
;;
go)
    go run $1
;;
rs)
    cargo run
;;
js)
    node $1
;;
*)
    print "Unknown file type, sorry"
;;
esac
